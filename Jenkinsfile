pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3 --version'
                sh 'echo "Hello World 3"'
            }
        }
        stage('test') {
            steps {
                sh 'python3 --version'
                sh 'echo "Testing"'
            }
        }
    }
}
